#include "Pods-Tests-Trim.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Trim" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/FBSnapshotTestCase" "${PODS_ROOT}/Headers/Trim"
OTHER_LDFLAGS = -ObjC ${PODS_TESTS_TRIM_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}